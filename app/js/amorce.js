/**
 * amorce.js — STUB DE PRODUCTION, écrit par tools/build.mjs.
 *
 * En développement, ce module pointe vers un persona du dépôt pour que le
 * moteur tourne sur de vraies données. Ce fichier-là n'est JAMAIS publié : le
 * build le remplace par ce stub.
 *
 * `AMORCE === null` veut dire : « cette app n'a pas de profil de départ ».
 * L'utilisateur en crée un (onboarding) ou en importe un (écran Données).
 */
export const AMORCE = null;
